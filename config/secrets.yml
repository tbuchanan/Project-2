# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 4ba6122c1ac7d0517c043a21bcf0c0eb5cf75cd8dfb4a796c95ebbfa881fd77de017a47a2e2f1525772688b50f3202c6e0bfff54044101acc245fa6f16f0e8b7

test:
  secret_key_base: 83a418ba72bf96577a8d7b9f06c652cadd47ca79eee7b3123f441e939ae71d7f52e7c7fc4fd65826f865aa35c389fbbde3e1c94f259464c017789c4fac3c43c7

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  devise_key_base: <%= ENV["DEVISE_KEY_BASE"] %>
  s3_bucket_name: <%= ENV["S3_BUCKET_NAME"] %>
  aws_access_key_id: <$= ENV ["AWS_ACCESS_KEY_ID"] %>
  aws_secret_access_key: <$= ENV ["AWS_SECRET_ACCESS_KEY"] %>
